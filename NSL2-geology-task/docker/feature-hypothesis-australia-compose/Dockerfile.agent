FROM python:3.12-slim

RUN python -m pip install --no-cache-dir \
    "mcp>=1.1.0" \
    "omegaconf>=2.3.0" \
    "loguru>=0.7"

WORKDIR /workspace
ENTRYPOINT []
CMD ["sh", "-c", "tail -f /dev/null"]
